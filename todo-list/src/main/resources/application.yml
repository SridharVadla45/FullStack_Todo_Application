server:
  port : 8081
spring:
  datasource:
    username: postgres
    password: Password
    url: jdbc:postgresql://localhost:5432/TodoDb
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true

